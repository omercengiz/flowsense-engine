# Golden analysis fixtures

These fixtures protect the complete, versioned FlowSense analysis contract and
the deterministic behavior of the default analysis engine.

Review fixture changes as API or algorithm changes, not as routine snapshot
updates. A schema-breaking change requires a new schema version and a new
versioned fixture instead of overwriting an older contract fixture.
